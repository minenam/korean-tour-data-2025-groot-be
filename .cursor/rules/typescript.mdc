- 모든 TypeScript 파일은 반드시 타입을 명확히 지정합니다.
- `any` 타입 사용을 지양합니다.
- import 경로는 절대경로(`@/` 등) 또는 상대경로를 일관성 있게 사용합니다.
- 파일명은 카멜케이스 또는 케밥케이스로 작성합니다.
- 인터페이스는 `I` 접두어 없이 작성합니다.
  description:
  globs:
  alwaysApply: false

---
